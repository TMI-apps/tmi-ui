# Table component — ingest landing zone

Public API is wired from [`index.ts`](./index.ts) into `src/index.ts`. Implementation currently lives under `lesmateriaal-import/` until the **1.8.1** flatten (see [`docs/jobs/temp_job_library-hygiene/DEVELOPMENT_PLAN.md`](../../docs/jobs/temp_job_library-hygiene/DEVELOPMENT_PLAN.md)).

Consumer SSOT: [package README — TMI table](../../README.md#tmi-table). Extract-complete grid is **1.3.x+**.
