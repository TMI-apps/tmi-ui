# Table component — ingest landing zone

Source from Lesmateriaal lives under `lesmateriaal-import/`. **Phase 2 public API** is wired from [`index.ts`](./index.ts) into `src/index.ts`.

Grid (`tmiTable/table`), workspace, and overlay context are **not** package exports yet.

See [docs/jobs/temp_job_table-component-ingest/](../../docs/jobs/temp_job_table-component-ingest/) and `lesmateriaal-import/docs/COPY_MANIFEST.md`.
