/**
 * Layout constants used by table row chrome. App-wide `createAppTheme` stays in the consumer.
 */

/** Corner radius (px) for table row hover/drag-over shape. */
export const TABLE_ROW_CORNER_RADIUS_PX = 6;

/**
 * Anchor for `object-fit: cover` in hero headers — slightly above geometric center
 * so faces crop more naturally than `50% 50%`.
 */
export const COVER_IMAGE_OBJECT_POSITION = "50% 40%";
