---
"@tmi-packages/ui": minor
---

Add optional `rowCreate` on TMITable: a pinned viewport create row with cell commit and one-column newline paste. `onCreate` returns the new row id; pending styling stays consumer-owned.
