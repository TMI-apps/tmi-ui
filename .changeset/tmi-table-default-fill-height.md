---
"@tmi-packages/ui": minor
---

TMITable fills remaining workspace/standalone height when `maxHeight` is omitted. Pin with a number; opt out with `maxHeight={false}` for nested/content-sized tables. `useDatabaseViewerMaxHeight` is deprecated for this use case.
