node_modules
dist
playground/dist
coverage
*.tgz
.turbo
.DS_Store
*.log

# Local secrets — never commit
.env
.env.*
!.env.example

# design-sync: staged converter scripts, build output, and machine state.
# The durable set (config.json, NOTES.md, conventions.md, previews/, overrides/)
# is NOT listed here and IS committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
